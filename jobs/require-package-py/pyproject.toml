[project]
name = "require-package-py"
version = "0.1.0"
description = "Python job that imports humanize (tests uv package loading)"
requires-python = ">=3.10"
dependencies = [
    "humanize>=4.0.0",
]
